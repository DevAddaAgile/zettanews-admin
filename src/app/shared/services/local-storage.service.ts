import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LocalStorageService {
  // This is just a placeholder service to fix the compilation error
  // The actual functionality is now in the individual API services
}